# DeepSeek 使用说明

网页里新增了「DeepSeek AI 文本整理」区域。

使用流程：

1. 在左侧输入文字，或上传 docx/pdf/txt
2. 选择模式：作业简写、课堂笔记、去AI味、压缩一半
3. 点击「AI整理文字」
4. 整理结果会自动回填到文本框
5. 点击预览、生成图片或生成 PDF

API Key 有两种填法：

- 推荐：第一次运行 `一键启动.bat` 时粘贴，会保存到 `backend/.env`
- 临时：直接在网页 DeepSeek 输入框粘贴

注意：DeepSeek 只整理文字；真正的手写效果来自你选择的 TTF 字体和页面里的扰动参数。
